rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // 誰でも読み取り可能にし、ログインしているユーザーのみ書き込みを許可する
    match /{allPaths=**} {
      allow read;
      allow write: if request.auth != null;
    }
  }
}